services:
  rust-bs:
    build:
      context: https://github.com/boxifyuser/rust-bs.git#main
      dockerfile: Dockerfile
    restart: unless-stopped
    # Portas: configure na aba Ports do Easypanel (nao aqui — evita conflito)
    volumes:
      - rust_data:/home/steam/rust
    environment:
      RCON_PASSWORD: "19541954"
      SERVER_HOSTNAME: "BICHO SOLTO BRASIL | SOLO"
      SERVER_SEED: "836891193"
      WORLD_SIZE: "1800"
      MAX_PLAYERS: "200"
      SERVER_PORT: "28015"
      RCON_PORT: "28016"
      QUERY_PORT: "28017"
      APP_PORT: "28082"
      SERVER_IDENTITY: "rst"
      RUST_CARBON_ENABLED: "1"
      SERVER_PUBLIC_IP: "168.231.91.245"
      STEAM_GSLT: "C69D8AFA1A2DB3967B6040DDC576EA9F"

volumes:
  rust_data:
